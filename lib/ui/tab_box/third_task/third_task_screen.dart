import 'package:fifth_exam/app/app.dart';
import 'package:flutter/material.dart';

class ThirdTaskScreen extends StatelessWidget {
  const ThirdTaskScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Third Task Screen"),
      ),
    );
  }
}
